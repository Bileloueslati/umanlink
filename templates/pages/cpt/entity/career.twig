
{% set group = post.meta('career') %}

<section class="lg:py-16 py-10 relative">

<div class="container mx-auto">

<div class="lg:grid lg:grid-cols-12 flex flex-col-reverse gap-x-16 gap-y-10 mt-16 lg:mt-0 items-center">

<div class="lg:col-span-7">

<span class="small_title">{{ group.small_title }}</span>

<h2 class="text-3xl mb-4 text-secondary">{{ group.title }} </h2>

<p class="lg:text-lg">{{ group.description }} </p>

</div>

<div class="lg:col-span-5">

<img src="{{ group.image }}" class="pointer-events-none max-h-[400px] relative lg:right-0 right-[-40px]" alt="{{ group.title }}" />

</div>

</div>
</section>